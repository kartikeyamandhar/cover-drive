# GPU stack, installed on the RunPod pod ONLY (needs CUDA). The Mac never installs these.
#
# USE A COHERENT, TESTED ENVIRONMENT (an Unsloth-official RunPod template or Unsloth's
# Colab). On a generic PyTorch image, a plain `unsloth` install pulls a torch preview that
# Unsloth's compiled kernels do not match -> it falls back to a Python path whose tokenizer
# cannot be pickled by `datasets` ('cannot pickle ConfigModuleInstance'), which blocks the
# tokenization step. The token/to_dict fixes in app/train/train.py are still required on any
# stack; only the torch<->unsloth kernel coherence needs the tested image.
unsloth
trl
peft
bitsandbytes
accelerate
datasets
wandb
# Repo runtime deps that unsloth does not provide:
pydantic-settings>=2.6,<3
structlog>=24.4,<25
huggingface-hub>=0.25,<1
